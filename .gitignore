# 빌드 산출물 — manifest.json 이 가리키는 dist/ 는 npm run build 로 생성한다
dist/

# 사내 공유용 zip — npm run pack 으로 생성한다
release/

# 의존성
node_modules/

# 도구 캐시
*.tsbuildinfo
.esbuild/

# 로그
*.log
npm-debug.log*
yarn-error.log*

# 에디터 / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# 로컬 환경
.env
.env.local
